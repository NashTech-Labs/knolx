#knoldus | knolx
=================================

#Clone and run the app(default database is H2):
=====================================================
       $ git clone git@github.com:knoldus/knolx.git
       $ cd knolx
       $ bin/activator run

#Run the all unit test : (Present Working Directory Should be knolx) :
===================================
        $ bin/activator test

